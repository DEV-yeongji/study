2021.01.13

일반적으로 웹 프로젝트는 3티어 방식으로 구성된다.
Presentation Tier(화면계층)는 화면에 보여주는 기술을 사용하는 영역이다.
Servlet/JSP 나 스프링 MVC가 담당하는 영역이다. 

Business Tier(비즈니스계층)는 순수한 비즈니스 로직을 담고있는 영역이다.
이 영역이 중요한 이뉴는 고객이 원하는 요구 사항을 반영하는 계층이기 때문이다.
이 영역은 주로 'xxxService' 같은 이름으로 구성하고, 메서드의 이름 역시 고객이 사용하는 용어를 그대로 하는것이 좋다.

Persistence Tier(영속계층 혹은 데이터 계층)는 데이터를 어떤 방식으로 보관하고, 사용하는가에
대한 설계가 들어가는 계층이다. 일반적인 경우에는 데이터베이스를 많이 이용하지만,
경우에 따라서 네트워크 호출이나 원격 호출 드으이 기술이 접목될 수 있다.

프로젝트를 위와 같이 3티어로 구성하는 가장 일반적인 설명은 유지보수에 대한 필요성 때문이다.
각 영역은 독립적으로 설계되어 나중에 특정한 기술이 변하더라도 필요한 부분을 전자제품의
부품처럼 쉽게 교환할 수 있게 하자는 방식이다. 따라서 각 영역은 설계 당시부터 영역을 구분하고,
해당 연결 부위는 인터페이스를 이용해서 설계하는 것이 일반적인 구성방식이다...

xml mapper의 작성
이번에 만들 프로젝트는 스프링 팀프로젝트인데 (현재작업중) 
mysql - mybatis를 이용할것이다. 자바파일 어노테이션이 아니라 xml로 작성할 예정이다.

mapper 작성시 namespace 속성값을 인터페이스와 동일한 이름을 주는 것에 주의하고, 
<select>태그의 id 속성값은 메서드의 이름과 일치하게 작성한다.
또한 CDATA는 xml에서 부등호를 사용하기 위해 사용한다.

★mapper ?
sql문을 저장하는 존재이다. dao클래스를 사용하는 sql을 작성해야한다.
xml , 인터페이스 를 이용한다..
src - main - resources 에 보통 폴더를 만든다.

-xml을 사용하는경우
xml로 작성된 mapper의 위치를 결정한다. xml mapper파일을 작성하고 필요한 dtd를 추가한다.

http://www.mybatis.org/mybatis-3/ko/getting-started.html 를 참고하여 dtd를 작성한다..

#select
mybatis에서 가장 많이 사용하는 엘레멘트..
어플리케이션은 데이터를 수정하는 기능보다 데이터 조회를 주로 한다.
<select id="쿼리이름" parameterType="파라미터 변수" resultType="저장될공간">
	select * from 테이블 where 
</select>



출처: https://zzangprogrammer.tistory.com/211 [개인공간]


